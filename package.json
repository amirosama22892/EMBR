{
  "name": "embr-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "jsdom integration tests for the EMBR frontend (embr.html). Not shipped to Azure; used by CI and local runs only.",
  "scripts": {
    "test": "node test_frontend.js"
  },
  "devDependencies": {
    "jsdom": "^24.1.3"
  }
}
